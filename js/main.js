// Main entry point for the application
document.addEventListener('DOMContentLoaded', () => {
  // Initialize the sketch manager
  new SketchManager();
  
  console.log('LLAL Generator initialized');
}); 